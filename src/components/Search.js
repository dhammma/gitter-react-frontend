import React from 'react'

const Search = () => (
    <div className="rooms-search">
        <input
            className="rooms-search-field"
            type="search"
            placeholder="Search"
            autoComplete="off"
        />
    </div>
)

export default Search